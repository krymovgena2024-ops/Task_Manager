{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: FastAPI Module",
            "type": "debugpy",
            "request": "launch",
            "module": "app.main", 
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "console": "integratedTerminal"
        }
    ]
}